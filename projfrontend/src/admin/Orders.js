import React from 'react';
import Base from '../core/Base';

const Orders = () => {
  return (
    <Base>
        <h1 className="text-white">Manage all Orders</h1>
    </Base>
  )
};

export default Orders;